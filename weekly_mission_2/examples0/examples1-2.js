// Ejemplo 2: for Each para culcular la suma de todos los
// elemtos de una lista
//

let sum = 0;
const numbers2 = [1,2,3,4,5];
numbers2.forEach(num => sum += num)
console.log("Ejemplo 2: Calculo de la suma de los elementos de la lista")
console.log(sum)
